import { style } from '@vanilla-extract/css';

export const payloadPreviewStyle = style({
  resize: 'vertical',
  fontFamily:
    'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace',
  fontSize: '0.75rem',
  lineHeight: 1.5,
  whiteSpace: 'pre',
});
